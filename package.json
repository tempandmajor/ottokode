{
  "name": "ottokode",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "web-app",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web-app",
    "dev:web": "npm run dev --workspace=web-app",
    "dev:desktop": "npm run tauri:dev",
    "build": "npm run build:shared && npm run build --workspace=web-app",
    "build:web": "npm run build:shared && npm run build --workspace=web-app",
    "build:shared": "npm run build --workspace=shared",
    "build:production": "NODE_ENV=production npm run build:shared && npm run build:production --workspace=web-app",
    "tauri": "tauri",
    "tauri:dev": "npm run build:shared && tauri dev",
    "tauri:build": "tauri build",
    "tauri:build:debug": "tauri build --debug",
    "tauri:build:release": "npm run build:shared && npm run build:tauri --workspace=web-app && tauri build",
    "clean": "rm -rf web-app/.next web-app/out src-tauri/target/release/bundle shared/dist",
    "clean:all": "rm -rf web-app/.next web-app/out web-app/node_modules shared/node_modules shared/dist node_modules src-tauri/target",
    "type-check": "npm run type-check --workspace=shared && npm run type-check --workspace=web-app",
    "lint": "npm run lint --workspace=shared && npm run lint --workspace=web-app",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:integration": "node scripts/run-integration-tests.js",
    "test:integration:quick": "node scripts/run-integration-tests.js --quick",
    "test:api": "node scripts/run-integration-tests.js --skip-build --skip-compatibility --skip-environment",
    "test:build": "node scripts/run-integration-tests.js --skip-api --skip-compatibility --skip-environment",
    "test:compatibility": "node scripts/run-integration-tests.js --skip-api --skip-build --skip-environment",
    "test:environment": "node scripts/run-integration-tests.js --skip-api --skip-build --skip-compatibility",
    "health": "node scripts/quick-health-check.js",
    "postinstall": "npm run build:shared",
    "release:prepare": "npm run clean && npm run build:production && npm run tauri:build:release",
    "release:github": "gh release create v$(node -p \"require('./package.json').version\") src-tauri/target/release/bundle/dmg/*.dmg src-tauri/target/release/bundle/msi/*.msi src-tauri/target/release/bundle/appimage/*.AppImage --title \"Ottokode v$(node -p \"require('./package.json').version\")\" --notes \"🎉 Ottokode v$(node -p \"require('./package.json').version\") - AI-powered IDE for modern development\" --repo tempandmajor/ottokode"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.62.0",
    "@google/generative-ai": "^0.24.1",
    "@mistralai/mistralai": "^1.10.0",
    "@monaco-editor/react": "^4.7.0",
    "@supabase/supabase-js": "^2.57.4",
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-fs": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.3.0",
    "@types/crypto-js": "^4.2.2",
    "cohere-ai": "^7.19.0",
    "crypto-js": "^4.2.0",
    "immer": "^10.1.3",
    "jsdom": "^27.0.0",
    "openai": "^5.20.2",
    "zustand": "^5.0.8"
  },
  "devDependencies": {
    "@jest/types": "^30.0.5",
    "@tauri-apps/cli": "^2.0.0",
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "autoprefixer": "^10.4.21",
    "concurrently": "^8.2.2",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^30.1.3",
    "jest-environment-jsdom": "^30.1.2",
    "ts-jest": "^29.4.4",
    "typescript": "^5.6.3"
  }
}
